#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - Compute absolute value
 * @n: variable
 *
 * Return: n = last digitbr
 */
int  print_last_digit(int n)
{
	if (n < 0)
	{
		n = n * -1;
		_putchar(n % 10 + '0');

	}
	else
	{
		_putchar(n % 10 + '0');
	}
		
		return (INT_MIN);
}
